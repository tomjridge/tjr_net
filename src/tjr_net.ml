(** Basic networking utils; safe to open this module (some modules
   have common names tho (eg Generic) and may clobber those from elsewhere) *)

module Net_intf = Net_intf

module Generic = Generic

module Lwt_ = Lwt_

module Unix_ = Unix_
